<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_mobile"
    app:startDestination="@id/dejarBusFragment">

    <fragment
        android:id="@+id/dejarBusFragment"
        android:name="com.example.parqueodebuses.DejarBusFragment"
        android:label="fragment_dejar_bus"
        tools:layout="@layout/fragment_dejar_bus" />
    <activity
        android:id="@+id/logInActivity"
        android:name="com.example.parqueodebuses.LogInActivity"
        android:label="activity_log_in"
        tools:layout="@layout/activity_log_in" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.example.parqueodebuses.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
</navigation>